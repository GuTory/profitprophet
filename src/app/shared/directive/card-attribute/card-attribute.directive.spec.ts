import { CardAttributeDirective } from './card-attribute.directive';

describe('CardViewDirective', () => {
  it('should create an instance', () => {
    const directive = new CardAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
