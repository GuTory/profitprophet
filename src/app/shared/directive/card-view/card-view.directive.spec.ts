import { CardViewDirective } from './card-view.directive';

describe('CardViewDirective', () => {
  it('should create an instance', () => {
    const directive = new CardViewDirective();
    expect(directive).toBeTruthy();
  });
});
