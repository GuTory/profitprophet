import { PaginationButtonDirective } from './pagination-button.directive';

describe('PaginationButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationButtonDirective();
    expect(directive).toBeTruthy();
  });
});
